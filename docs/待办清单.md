# 待办清单

> 长期待办记录，用于避免功能遗漏。新增待办时按下方格式追加并 git commit。

## 待接入：应用 C 端首页 → 链接器 / 首页跳转（2026-09-18 记录）

- **现状**：直播（live）、门店（store）、内容（content）等应用还没有 C 端 uni 页面，装修组件链接器（`web-admin/src/views/customer/apps/design/linkCatalog.js`「行业应用首页」分类）与设计中心首页跳转（`DesignHome.vue` HOME_APPS）暂未登记这些应用。
- **触发条件**：对应应用 C 端首页开发完成后，需同步：
  1. `linkCatalog.js`「行业应用首页」分类加一行 `{ label: 'XX首页', value: '/pages/xx/index' }`（链接器配置驱动，自动出现）
  2. `DesignHome.vue` HOME_APPS 加 `{ appCode: 'xx', name: 'XX', icon: '...' }` + groupsFor 页面分组（首页跳转自动出现）
  3. 构建 `npm run build:admin` + `npm run verify`，涉及 web-app 时构建 h5 并同步 `server/public/card`
- **机制已就绪**：链接器/首页跳转均为配置驱动，无需改组件逻辑，加配置即可。
- **参考**：已登记的智能名片首页 `/pages/cardMain/home`、360全景首页 `/pages/index/index`、商城首页 `/pages/mall/index`。

## ~~待迁移：应用 C 端首页路径语义化（方案B）~~ ✅ 已完成（2026-09-18）

- **已实施**：`pages/index/index` → `pages/panorama/index` 迁移完成（页面文件 git mv + pages.json + design.js HOME_PAGE_MAP + design.test.js + linkCatalog.js + DesignHome.vue + DB home_pages.panorama 迁移）。
- **命名规范（生效中）**：未来新增行业应用 C 端首页统一 `/pages/<appCode>/index`（直播 `/pages/live/index`、门店 `/pages/store/index`、内容 `/pages/content/index`）。
- **⚠️ 隔离红线（勿动）**：`web-admin/src/views/admin/channel/ChannelDefaults.vue`、`server/src/routes/channel.js`、`server/src/services/wx-component.js` 里的 `pages/index/index` 是**微信小程序码默认落地页占位**（渠道语义），与全景页无关，**禁止改**。
- **验证**：全仓业务引用清零；H5+管理端+小程序三端构建通过；前端 38 项、后端 351/351 全绿；浏览器实测首页跳转回显与 C 端新路径页面正常。
