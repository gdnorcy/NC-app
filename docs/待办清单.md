# 待办清单

> 长期待办记录，用于避免功能遗漏。新增待办时按下方格式追加并 git commit。

## 待接入：应用 C 端首页 → 链接器 / 首页跳转（2026-09-18 记录）

- **现状**：直播（live）、门店（store）、内容（content）等应用还没有 C 端 uni 页面，装修组件链接器（`web-admin/src/views/customer/apps/design/linkCatalog.js`「行业应用首页」分类）与设计中心首页跳转（`DesignHome.vue` HOME_APPS）暂未登记这些应用。
- **触发条件**：对应应用 C 端首页开发完成后，需同步：
  1. `linkCatalog.js`「行业应用首页」分类加一行 `{ label: 'XX首页', value: '/pages/xx/index' }`（链接器配置驱动，自动出现）
  2. `DesignHome.vue` HOME_APPS 加 `{ appCode: 'xx', name: 'XX', icon: '...' }` + groupsFor 页面分组（首页跳转自动出现）
  3. 构建 `npm run build:admin` + `npm run verify`，涉及 web-app 时构建 h5 并同步 `server/public/card`
- **机制已就绪**：链接器/首页跳转均为配置驱动，无需改组件逻辑，加配置即可。
- **参考**：已登记的智能名片首页 `/pages/cardMain/home`、360全景首页 `/pages/panorama/index`、商城首页 `/pages/mall/index`。

## 商城 C 端一期开发（2026-09-18 确认「按推荐」）

**范围（一期 MVP）**：商品浏览/详情/购物车/下单（快递+到店自提）/支付/订单列表详情；门店选择器同步纳入一期。
**已确认决策**：
1. API 基址 = 方案C：抽 `createApiClient(baseURL)` 公共请求层（token/401/解包一份代码），`mallApi = createApiClient('/api/mall')`；服务端 `/api/mall` 独立 router，整体挂 `requireGoodsApp` C 端变体（一个中间件全覆盖，权限不漏）。cardApi 保持不动（兼容现网页面）。
2. 购物车 = 服务端（登录态，H5/小程序同步），新表 `goods_cart`。
3. 一期商品类型 = 普通 + 虚拟；卡密/礼物/卡券二期。
4. 支付 = 先 mock-pay + 现有 payment 回调框架跑通闭环；正式微信支付（商户号）另行排期。
5. 到店自提 = 门店选择器读 store 表；营业时间校验二期（store.business_time）。
6. 门店收款 = 一期统一线上支付；confirm_pay_enabled（门店确认收款）二期。

**门店×商城结合（已确认）**：
- 定位：store 门店管理 = 扩充商品管理的「总部+门店」两级商城体系（总部统一商品库，门店=销售/自提/核销/结算终端）。
- 数据模型：新增 `goods_store` 关系表（goods_id+store_id+门店价/门店库存/上下架），不污染 goods 主表；按 store.price_mode/stock_mode/shelf_mode 三种模式生效。
- 二期：goods_store 关系表 + store_admin 按 store_id 隔离订单/售后 + 核销码；三期：门店结算/提现 + 门店 C 端（复用商城页面+store_id 过滤）。
- 门店数量 = 总后台授权配额 + 超量可购买（nshop 机制，已复刻），不影响一期。

**C 端页面清单**（web-app/src/pages/mall/*）：商品列表（分类+搜索）、商品详情（SKU/库存/配送）、购物车、下单页（快递/到店自提选门店）、订单列表/详情。路径 `/pages/mall/*`，`/mall` 入口已就绪。
**权限缝隙**：C 端页面级 goods 应用校验（见下条「商城 C 端独立 H5 规划」），一期一并补。

## 商城 C 端独立 H5 规划（2026-09-18 预留入口，正式开发时实施）

- **入口已预留**：`/mall`（server/src/app.js）与 `/card` 共用 uni H5 产物，裸 `/mall` 302 直达 `#/pages/mall/index`（保留 query），已按 goods 应用校验开通（未开通 → 403 未开通页）。测试：`server/test/app-entry.test.js`（+5 用例，15/15）。
- **正式独立构建时清单**：
  1. mallDist 从 card 产物改为独立构建目录（一行）；
  2. 登录态打通：uni storage token 与独立产物存储 key / 校验机制一致；
  3. API 基址：cardApi 写死 localhost:3000（开发配置），独立产物统一 /api 相对或环境注入；
  4. 租户解析与 /pano 对齐（一期 ?tid=，二期子域名按 Host）；
  5. SW/缓存按需自配（注意 AGENTS.md 问题 2/9 缓存坑）；
  6. URL 稳定：产物用 ./assets 相对路径，换挂载点安全。
- **⚠️ 权限缝隙（商城/内容 C 端有真实功能前必须补）**：`/card` 入口只校验「智能名片」开通，站内 hash 直达 `#/pages/mall/index`、`#/pagesReads/...` 等页面**未校验对应应用（goods/content）开通**。目前占位页无害；真实功能上线时需 uni 端页面级应用校验或服务端接口层校验兜底。

## ~~待迁移：应用 C 端首页路径语义化（方案B）~~ ✅ 已完成（2026-09-18）

- **已实施**：`pages/index/index` → `pages/panorama/index` 迁移完成（页面文件 git mv + pages.json + design.js HOME_PAGE_MAP + design.test.js + linkCatalog.js + DesignHome.vue + DB home_pages.panorama 迁移）。
- **命名规范（生效中）**：未来新增行业应用 C 端首页统一 `/pages/<appCode>/index`（直播 `/pages/live/index`、门店 `/pages/store/index`、内容 `/pages/content/index`）。
- **⚠️ 隔离红线（勿动）**：`web-admin/src/views/admin/channel/ChannelDefaults.vue`、`server/src/routes/channel.js`、`server/src/services/wx-component.js` 里的 `pages/index/index` 是**微信小程序码默认落地页占位**（渠道语义），与全景页无关，**禁止改**。
- **验证**：全仓业务引用清零；H5+管理端+小程序三端构建通过；前端 38 项、后端 351/351 全绿；浏览器实测首页跳转回显与 C 端新路径页面正常。
