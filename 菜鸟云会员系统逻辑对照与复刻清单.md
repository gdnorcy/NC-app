# 菜鸟云「会员」系统逻辑对照与复刻清单

> 对标：菜鸟云「秘联」项目（appletid=19）「用户」菜单 9 个子页面
> 本文档基于当时逐页实测 + 当前代码核实（2026-09-15），用于指导后续补齐与扩展。

---

## 一、菜鸟云 9 个子页面功能总览

| # | 页面 | 对标逻辑要点 |
|---|------|-------------|
| 1 | 数据统计 | 会员总数/新增/活跃、等级分布、近 N 日趋势 |
| 2 | 用户管理 | 会员列表：搜索/筛选/身份/等级/开卡状态、打标签、导出 |
| 3 | 用户标签 | 标签 CRUD + 按标签筛选/批量打标 |
| 4 | 用户组 | 手动/规则分组，组内批量操作（打标、发券等） |
| 5 | 消费流水 | 会员在商城的消费记录（对接订单，含金额/商品/时间） |
| 6 | 积分流水 | 积分获得/使用明细（签到、消费、权益兑换） |
| 7 | 会员设置 | 开卡开关、到期提醒天数、展示字段（卡号/到期）、权益配置 |
| 8 | 会员等级 | 等级 CRUD：升级模式（消费满额/申请/购买）、权益、外观 |
| 9 | 申请记录 / 开卡记录 | 申请审核流 + 开卡流水 |

---

## 二、逐项对照：对标逻辑 vs 我们实现 vs 差异

### 1. 数据统计
- **对标逻辑**：会员总数、等级分布、趋势。
- **我们实现**：`GET /member/summary`（总数/等级分布/即将到期/近30天消费会员数），后台「数据统计」Tab 卡片展示。
- **状态**：✅ 已完成
- **差异/待补**：菜鸟云有「新增会员趋势折线图」，我们只有数字卡片，无趋势图（可后续加 ECharts）。

### 2. 用户管理（会员列表）
- **对标逻辑**：搜索（昵称/手机/卡号）、筛选（等级/标签/身份/开卡状态）、打标签、导出 CSV。
- **我们实现**：`GET /member/users`（keyword/levelId/labelId/identityType/cardStatus 筛选 + 分页 + CSV 导出）；后台「会员列表」Tab：搜索框、等级/标签/身份/状态筛选、打标签弹窗（setUserLabels）、导出按钮。
- **状态**：✅ 已完成（标签筛选、打标签均已接）
- **差异/待补**：无重大差异。

### 3. 用户标签
- **对标逻辑**：标签管理页（新建/删除标签）+ 会员批量打标 + 按标签筛选。
- **我们实现**：
  - 后端 ✅：`member_labels`/`member_user_labels` 表 + `listLabels/addLabel/deleteLabel/setUserLabels/userLabels`；路由 `GET/POST/DELETE /member/labels`、`PUT /member/users/:id/labels`。
  - 前端 ⚠️ **部分**：会员列表已接标签筛选 + 单用户打标签弹窗；**独立「标签管理」UI（新建/删除标签）未做**。
- **状态**：🟡 后端完成，前端管理页漏项
- **待补**：后台加「标签管理」入口（或在会员列表页头加标签管理弹窗：新建/删除标签），即可闭环。

### 4. 用户组
- **对标逻辑**：创建分组（按规则自动入组 / 手动添加）+ 组内批量操作（打标、群发等）。
- **我们实现**：❌ 完全未建（无表、无接口、无页面）。
- **状态**：🔴 未做（一期范围裁剪）
- **原因**：一期方案 4 个拍板点未含用户组；且组内批量操作多依赖商城权益（发券/群发），属待接入范畴。
- **待补（建议）**：若要做，建 `member_groups`/`member_group_users` 两表，支持规则分组（按等级/标签/身份）+ 手动分组；组内操作先做「批量打标签」，发券等随商城接入。

### 5. 消费流水
- **对标逻辑**：会员在商城消费产生的订单流水（金额/商品/时间/余额变动）。
- **我们实现**：
  - 表 ✅：`member_logs`（type=consume），购买会员开卡时写入 `-金额` 流水（openCardByOrder 内）。
  - 路由 ✅：`GET /member/logs`（筛选 + 导出 CSV）。
  - 页面 ❌：后台无独立「消费流水」Tab。
- **状态**：🟡 数据链路就绪，页面未做
- **原因**：菜鸟云消费流水**数据来源是商城订单**；商城权益（包邮/秒杀/拼团/砍价/领券/兑换/余额充值）已拍板「待接入」，故流水只有「购买会员」一种来源，独立页暂无业务价值。
- **待补（建议）**：商城接入后，订单支付成功统一写 `member_logs(consume)`（含商品名/订单号），再做独立「消费流水」Tab + 导出。

### 6. 积分流水
- **对标逻辑**：积分获得/使用明细（签到、消费、权益兑换）。
- **我们实现**：
  - 表 ✅：`member_score_logs`（type get/use，签到 +2 已写）。
  - 路由 ✅：`GET /member/score-logs`（列表 + CSV）。
  - 页面 ⚠️：会员列表/详情可看？—— 独立积分流水 Tab 未做（与消费流水同批待补）。
- **状态**：🟡 后端就绪，页面未做
- **待补**：随「消费流水」Tab 一起做「积分流水」Tab（签到/消费/兑换分类展示）。

### 7. 会员设置
- **对标逻辑**：开卡开关、到期提醒天数、卡面展示（卡号/到期）、权益配置 JSON。
- **我们实现**：`member_settings`（tenant_id 主键单行：card_enabled/expire_remind_days/show_name/show_expire/permissions）；后台「会员设置」Tab 完整表单。
- **状态**：✅ 已完成
- **差异/待补**：菜鸟云设置项更细（如卡面文案/样式）；我们的 permissions JSON 结构保留待接入。

### 8. 会员等级
- **对标逻辑**：等级 CRUD；升级模式 `consume`（消费满额自动升级）/`apply`（申请开通）/`buy`（直接购买）；权益 JSON（折扣/积分返还/积分倍数/查看价格）；外观（背景/文字/图标）。
- **我们实现**：`member_levels` 全字段 + 后台「会员等级」Tab CRUD；C 端购买/申请/当前等级态。
- **状态**：✅ 已完成
- **差异/待补**：菜鸟云「消费满额自动升级」需要消费流水触发升级任务——我们**尚无升级触发逻辑**（consume_amount 配置了，但升级动作未实现，依赖商城消费数据）。**待商城接入后补升级 job**。

### 9. 申请记录 / 开卡记录
- **对标逻辑**：申请审核流（pending→approved/rejected 带原因）+ 开卡流水。
- **我们实现**：`member_apply` + `GET /member/applies` + `POST /member/applies/:id/review`；后台「申请记录」「开卡记录」两个 Tab。
- **状态**：✅ 已完成

---

## 三、核心业务闭环（已实现逻辑）

```
开卡链路：
  购买 → POST /card/member/buy（生成订单 productType=member_card）
       → 支付成功（mock-pay / handlePaymentSuccess）
       → member.openCardByOrder(order) → 写 member_user + 消费流水(-金额)
  申请 → POST /card/member/apply（name/phone/levelId）
       → 后台审核 approve → 自动开卡 / reject 带原因
等级升级：consume 模式（累计消费满额自动升级）—— ⚠️ 升级触发待商城接入
积分：
  每日签到 → POST /card/member/sign → +2 分（按日期判重）
  积分流水 → member_score_logs（get/use）
```

---

## 四、缺口汇总（按优先级）

| 优先级 | 缺口 | 依赖 | 工作量评估 |
|--------|------|------|-----------|
| P0 | 用户标签管理 UI（新建/删除标签） | 无（后端已就绪） | 小：会员列表页头加标签管理弹窗 |
| P0 | 消费流水 + 积分流水 Tab | 无（后端已就绪） | 中：两个列表页 + 筛选 + 导出 |
| P1 | 商城权益接入（包邮/秒杀/拼团/砍价/领券/兑换/余额充值） | 订单/商品体系 | 大（已拍板待接入） |
| P1 | 消费满额自动升级触发（consume_amount） | 商城消费数据 | 中：支付成功处加升级判断 |
| P1 | 会员数据统计趋势图 | 无 | 小：ECharts 折线 |
| P2 | 用户组（规则/手动分组 + 批量打标） | 标签体系 | 中 |
| P2 | 到期提醒触达（expire_remind_days） | 消息体系 | 小 |

---

## 五、待确认事项

- 用户标签管理 UI、消费/积分流水 Tab：**是否本轮补做？**（后端已就绪，纯前端工作量）
- 用户组：做不做？若做，组内操作一期先做「批量打标签」可否？
- 其余（商城权益/自动升级/趋势图/到期提醒）：维持「待接入」还是排期？
